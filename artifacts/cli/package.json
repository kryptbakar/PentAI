{
  "name": "pentai",
  "version": "1.0.0",
  "description": "Autonomous, authorization-first web pentesting from your terminal.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "pentai": "dist/pentai.mjs"
  },
  "files": ["dist"],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node ./build.mjs",
    "start": "node ./dist/pentai.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepublishOnly": "node ./build.mjs"
  },
  "keywords": ["pentest", "security", "cli", "nuclei", "nmap", "recon", "vulnerability-scanner"],
  "comment": "The engine and the Anthropic SDK are bundled into dist/pentai.mjs at build time, so the published package has zero runtime dependencies and installs standalone.",
  "devDependencies": {
    "@workspace/engine": "workspace:*",
    "@types/node": "catalog:",
    "esbuild": "0.27.3"
  }
}
